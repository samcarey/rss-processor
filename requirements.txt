Flask>=3.0.0
SQLAlchemy>=2.0.36
feedparser>=6.0.10
feedgen>=1.0.0
requests>=2.31.0
pydub>=0.25.1
PyYAML>=6.0.1
numpy>=2.0  # raw fingerprint matching (np.bitwise_count)

# System dependencies (brew): ffmpeg, chromaprint (fpcalc), whisper-cpp
# Whisper model: download ggml-small.en.bin from
# https://huggingface.co/ggerganov/whisper.cpp to data/models/
